{
  "name": "mock-http-output",
  "version": "1.0.0",
  "description": "Mock server for HTTP Output API (Aceni-style PDF upload). Returns 200 or configurable errors by path.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  }
}
